{% extends 'tailwind_2_layout.html.twig' %}

{%- block form_label -%}
    {%- set label_attr = label_attr|merge({ class: label_attr.class|default('block text-slate-300') }) -%}
    {{- parent() -}}
{%- endblock form_label -%}

{%- block form_help -%}
    {%- set help_attr = help_attr|merge({ class: help_attr.class|default('mt-1 text-slate-400') }) -%}
    {{- parent() -}}
{%- endblock form_help -%}
