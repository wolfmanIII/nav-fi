{% extends 'tailwind_2_layout.html.twig' %}

{%- block form_label -%}
    {%- set label_attr = label_attr|merge({ class: label_attr.class|default('block text-[10px] uppercase tracking-widest font-rajdhani font-bold text-slate-400 mb-1') }) -%}
    {{- parent() -}}
{%- endblock form_label -%}

{%- block form_help -%}
    {%- set help_attr = help_attr|merge({ class: help_attr.class|default('text-[10px] text-slate-500 mt-1 font-rajdhani uppercase tracking-wider') }) -%}
    {{- parent() -}}
{%- endblock form_help -%}
