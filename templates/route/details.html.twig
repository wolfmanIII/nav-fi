{% extends 'base.html.twig' %}

{% block title %}Nav-Fi - Route{% endblock %}

{% block pageContent %}

    <div class="flex flex-col items-stretch gap-6">
        <div class="relative rounded-xl border border-cyan-500/30 bg-gradient-to-r from-slate-900 via-slate-800 to-cyan-900 shadow-lg">
            <div class="absolute inset-0 rounded-xl opacity-30 mix-blend-screen pointer-events-none"
                 style="background: radial-gradient(circle at 20% 20%, rgba(59,130,246,0.25), transparent 35%), radial-gradient(circle at 80% 30%, rgba(14,165,233,0.25), transparent 35%);">
            </div>
            <div class="relative p-6 flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4">
                <div class="flex items-start gap-4">
                    <div class="p-3 rounded-lg bg-cyan-500/10 border border-cyan-400/40">
                        {% include 'icons/routes.html.twig' %}
                    </div>
                    <div>
                        <p class="uppercase text-xs tracking-[0.25em] text-cyan-300/70">Route dossier</p>
                        <h1 class="text-3xl font-semibold text-cyan-100 leading-tight">{{ route.name }}</h1>
                        <p class="text-sm text-cyan-100/80">{{ route.description ?? '—' }}</p>
                    </div>
                </div>
                <div class="flex gap-2">
                    <a href="{{ path('app_route_edit', { id: route.id }) }}" class="btn btn-outline btn-primary btn-sm">Edit</a>
                    <a href="{{ mapUrl }}" target="_blank" rel="noopener" class="btn btn-outline btn-info btn-sm">Open TravellerMap</a>
                </div>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-4">
            <div class="rounded-xl border border-base-300 bg-base-200 p-4">
                <div class="text-xs uppercase tracking-[0.2em] text-slate-400">Ship</div>
                <div class="text-lg text-cyan-100">{{ route.ship.name }}</div>
            </div>
            <div class="rounded-xl border border-base-300 bg-base-200 p-4">
                <div class="text-xs uppercase tracking-[0.2em] text-slate-400">Campaign</div>
                <div class="text-lg text-cyan-100">{{ route.campaign ? route.campaign.title : '—' }}</div>
            </div>
            <div class="rounded-xl border border-base-300 bg-base-200 p-4">
                <div class="text-xs uppercase tracking-[0.2em] text-slate-400">Jump / Fuel</div>
                <div class="text-lg text-cyan-100">
                    {{ route.jumpRating ?? '—' }} / {{ route.fuelEstimate ?? '—' }}
                </div>
            </div>
        </div>

        <div class="rounded-xl border border-base-300 bg-base-200 p-4">
            <div class="text-xs uppercase tracking-[0.2em] text-slate-400 mb-2">TravellerMap</div>
            <iframe
                src="{{ mapUrl }}"
                class="w-full h-100 rounded-lg border border-base-300"
                loading="lazy">
            </iframe>
        </div>

        <div class="overflow-x-auto rounded-xl border border-slate-800 bg-slate-950/40 shadow-inner">
            <table class="table table-xs w-full border-separate border-spacing-0">
                <thead>
                <tr class="border-b border-slate-800">
                    <th class="font-rajdhani uppercase tracking-widest text-[11px] text-slate-400 bg-slate-900/50">#</th>
                    <th class="font-rajdhani uppercase tracking-widest text-[11px] text-slate-400 bg-slate-900/50">Hex</th>
                    <th class="font-rajdhani uppercase tracking-widest text-[11px] text-slate-400 bg-slate-900/50">Sector</th>
                    <th class="font-rajdhani uppercase tracking-widest text-[11px] text-slate-400 bg-slate-900/50">World</th>
                    <th class="font-rajdhani uppercase tracking-widest text-[11px] text-slate-400 bg-slate-900/50">UWP</th>
                    <th class="font-rajdhani uppercase tracking-widest text-[11px] text-slate-400 bg-slate-900/50 text-right">Jump</th>
                    <th class="font-rajdhani uppercase tracking-widest text-[11px] text-slate-400 bg-slate-900/50">Notes</th>
                    <th class="bg-slate-900/50"></th>
                </tr>
                </thead>
                <tbody class="divide-y divide-slate-800/50">
                {% if route.waypoints|length > 0 %}
                    {% for waypoint in route.waypoints %}
                        <tr class="hover:bg-emerald-500/5 transition-colors text-slate-300">
                            <td class="font-mono text-xs p-2">{{ waypoint.position }}</td>
                            <td class="font-mono text-xs p-2 border-r border-slate-800/50 font-bold text-emerald-400">{{ waypoint.hex }}</td>
                            <td class="font-mono text-xs p-2">{{ waypoint.sector ?? '—' }}</td>
                            <td class="font-mono text-xs p-2 font-bold">{{ waypoint.world ?? '—' }}</td>
                            <td class="font-mono text-xs p-2">{{ waypoint.uwp ?? '—' }}</td>
                            <td class="font-mono text-xs p-2 text-right text-emerald-300">{{ waypoint.jumpDistance ?? '—' }}</td>
                            <td class="font-mono text-xs p-2 text-slate-500 italic">{{ waypoint.notes ?? '—' }}</td>
                            <td class="text-right p-2">
                                {% if waypoint.hex %}
                                    <a class="btn btn-ghost btn-xs text-[10px] uppercase font-bold tracking-widest text-slate-400 hover:text-emerald-400 hover:bg-emerald-500/10"
                                       href="{{ path('app_route_details', { id: route.id }) }}?marker_hex={{ waypoint.hex|url_encode }}{% if waypoint.sector %}&marker_sector={{ waypoint.sector|url_encode }}{% endif %}">
                                        Map
                                    </a>
                                {% else %}
                                    <span class="opacity-30">—</span>
                                {% endif %}
                            </td>
                        </tr>
                    {% endfor %}
                {% else %}
                    <tr>
                        <td colspan="8" class="text-center text-slate-500 font-rajdhani uppercase tracking-widest text-xs py-8">
                            // No waypoints mapped
                        </td>
                    </tr>
                {% endif %}
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}
