<fieldset class="fieldset grid grid-cols-3 bg-base-200 border-base-300 gap-4 p-4 border rounded w-250">
    <legend class="fieldset-legend">
        {% include 'icons/income.html.twig' %} SERVICES Details
    </legend>

    <div class="col-span-3">
        <div class="relative overflow-hidden rounded-xl border border-cyan-500/30 bg-gradient-to-r from-slate-900 via-slate-800 to-cyan-900 shadow-lg">
            <div class="absolute inset-0 opacity-30 mix-blend-screen pointer-events-none"
                 style="background: radial-gradient(circle at 20% 20%, rgba(59,130,246,0.25), transparent 35%), radial-gradient(circle at 80% 30%, rgba(14,165,233,0.25), transparent 35%);">
            </div>
            <div class="relative p-4 flex items-start gap-3">
                <div class="p-3 rounded-lg bg-cyan-500/10 border border-cyan-400/40">
                    {% include 'icons/income.html.twig' %}
                </div>
                <div>
                    <p class="uppercase text-xs tracking-[0.25em] text-cyan-300/70">Income details</p>
                    <h3 class="font-semibold text-cyan-100 text-xl leading-tight">Service workorder</h3>
                    <p class="text-sm text-cyan-100/80">Scope, schedule, parts/materials and payment/liability clauses.</p>
                </div>
            </div>
        </div>
    </div>

    <div class="col-span-3 alert alert-info alert-outline shadow-sm flex items-start gap-2">
        <span class="pt-0.5 animate-spin-slow">{% include 'icons/radar.html.twig' %}</span>
        <p class="text-sm">Service work order brief: vessel/asset, scheduled window, scope, parts/materials, risks and payment/extras/liability clauses.</p>
    </div>

    <div>
        {{ form_label(detail_form.location) }}
        {{ form_widget(detail_form.location) }}
        {{ form_errors(detail_form.location) }}
    </div>
    <div>
        {{ form_label(detail_form.serviceType) }}
        {{ form_widget(detail_form.serviceType) }}
        {{ form_errors(detail_form.serviceType) }}
    </div>
    <div>
        {{ form_label(detail_form.requestedBy) }}
        {{ form_widget(detail_form.requestedBy) }}
        {{ form_errors(detail_form.requestedBy) }}
    </div>

    <div class="col-span-3 grid grid-cols-2 gap-4">
        {% set data = detail_form.vars.data is defined ? detail_form.vars.data : null %}
        {% set start_day = data ? data.startDay : null %}
        {% set start_year = data ? data.startYear : null %}
        {% set start_value = start_day is not null and start_year is not null ? start_day|format('%03d') ~ '/' ~ start_year : '' %}
        <div class="col-span-1" data-controller="imperial-date">
            {{ form_label(detail_form.startDate) }}
            {{ form_widget(detail_form.startDate.display, {
                attr: {
                    'data-imperial-date-prev-icon': '«',
                    'data-imperial-date-next-icon': '»',
                    'data-imperial-date-initial-day': start_day ?? '',
                    'data-imperial-date-initial-year': start_year ?? '',
                    'value': start_value,
                }
            }) }}
            {{ form_widget(detail_form.startDate.year) }}
            {{ form_widget(detail_form.startDate.day) }}
            {{ form_errors(detail_form.startDate) }}
        </div>
        {% set end_day = data ? data.endDay : null %}
        {% set end_year = data ? data.endYear : null %}
        {% set end_value = end_day is not null and end_year is not null ? end_day|format('%03d') ~ '/' ~ end_year : '' %}
        <div class="col-span-1" data-controller="imperial-date">
            {{ form_label(detail_form.endDate) }}
            {{ form_widget(detail_form.endDate.display, {
                attr: {
                    'data-imperial-date-prev-icon': '«',
                    'data-imperial-date-next-icon': '»',
                    'data-imperial-date-initial-day': end_day ?? '',
                    'data-imperial-date-initial-year': end_year ?? '',
                    'value': end_value,
                }
            }) }}
            {{ form_widget(detail_form.endDate.year) }}
            {{ form_widget(detail_form.endDate.day) }}
            {{ form_errors(detail_form.endDate) }}
        </div>
    </div>

    <div class="col-span-3">
        {{ form_label(detail_form.workSummary) }}
        {{ form_widget(detail_form.workSummary) }}
        {{ form_errors(detail_form.workSummary) }}
    </div>

    <div class="col-span-3 grid grid-cols-2 gap-4">
        <div>
            {{ form_label(detail_form.partsMaterials) }}
            {{ form_widget(detail_form.partsMaterials) }}
            {{ form_errors(detail_form.partsMaterials) }}
        </div>
        <div>
            {{ form_label(detail_form.risks) }}
            {{ form_widget(detail_form.risks) }}
            {{ form_errors(detail_form.risks) }}
        </div>
    </div>

    {% set pay_help_id = detail_form.paymentTerms.vars.id ~ '-help' %}
    <div class="col-span-3">
        <div class="flex items-center gap-2">
            {{ form_label(detail_form.paymentTerms) }}
            <label for="{{ pay_help_id }}" class="badge badge-outline badge-info cursor-pointer badge-xs">?</label>
        </div>
        {{ form_widget(detail_form.paymentTerms) }}
        {{ form_errors(detail_form.paymentTerms) }}
        <input type="checkbox" id="{{ pay_help_id }}" class="modal-toggle hidden" />
        <div class="modal" role="dialog">
            <div class="modal-box">
                <h3 class="font-bold">Payment terms</h3>
                <p class="py-2 text-sm">Service billing: deposits, milestones, acceptance criteria, and due dates.</p>
                <div class="modal-action">
                    <label for="{{ pay_help_id }}" class="btn btn-primary">Close</label>
                </div>
            </div>
        </div>
    </div>

    <div class="col-span-3 grid grid-cols-3 gap-4">
        <div>
            {{ form_label(detail_form.extras) }}
            {{ form_widget(detail_form.extras) }}
            {{ form_errors(detail_form.extras) }}
        </div>
        <div>
            {{ form_label(detail_form.cancellationTerms) }}
            {{ form_widget(detail_form.cancellationTerms) }}
            {{ form_errors(detail_form.cancellationTerms) }}
        </div>
        {% set liab_help_id = detail_form.liabilityLimit.vars.id ~ '-help' %}
        <div>
            <div class="flex items-center gap-2">
                {{ form_label(detail_form.liabilityLimit) }}
                <label for="{{ liab_help_id }}" class="badge badge-outline badge-info cursor-pointer badge-xs">?</label>
            </div>
            {{ form_widget(detail_form.liabilityLimit) }}
            {{ form_errors(detail_form.liabilityLimit) }}
            <input type="checkbox" id="{{ liab_help_id }}" class="modal-toggle hidden" />
            <div class="modal" role="dialog">
                <div class="modal-box">
                    <h3 class="font-bold">Liability limit</h3>
                    <p class="py-2 text-sm">Cap on liability for the provided service (damages, downtime, or losses). Leave blank if unlimited.</p>
                    <div class="modal-action">
                        <label for="{{ liab_help_id }}" class="btn btn-primary">Close</label>
                    </div>
                </div>
            </div>
        </div>
    </div>
</fieldset>
