{% extends 'base.html.twig' %}

{% block title %}Nav-Fi - Nav-Fi Asset AI{% endblock %}

{% block pageContent %}
    <div class="p-10">

        <header class="prose mb-4">
            <h1>Nav-Fi Asset AI</h1>
        </header>
        <p class="mt-4 mb-4">
            Chiedi a Nav-Fi informazioni sulla tua campagna(<code>Nav-Fi var/knowledge</code>).
        </p>

        <div class="grid gap-4 lg:grid-cols-[2fr,1fr]" data-controller="ai-console">

            {# COLONNA SINISTRA: chat #}
            <div class="card bg-base-200 shadow-md">
                <div class="card-body gap-3">

                    <div class="h-[420px] overflow-y-auto space-y-3 bg-base-100 rounded-md p-3"
                         data-ai-console-target="messages">
                        {# messaggi verranno inseriti via Stimulus #}
                        <p class="text-sm opacity-70">
                            Scrivi una domanda qui sotto. Nav-Fi userà solo i documenti indicizzati.
                        </p>
                    </div>

                    <form class="mt-2 flex flex-col gap-2"
                          data-action="submit->ai-console#submit">
                        <textarea
                            class="textarea textarea-bordered w-full min-h-24"
                            placeholder="Es: Spiegami le condizioni del mutuo standard..."
                            data-ai-console-target="input"
                        ></textarea>

                        <div class="flex items-center justify-between gap-2">
                            <button
                                type="submit"
                                class="btn btn-primary"
                                data-ai-console-target="button">
                                Invia domanda
                            </button>

                            <span class="text-xs opacity-70">
                                Engine: <b>NAV-FI</b>
                            </span>
                        </div>
                    </form>
                </div>
            </div>

            {# COLONNA DESTRA: stato & note #}
            <div class="card bg-base-200 shadow-sm">
                <div class="card-body text-sm gap-3">
                    <h2 class="card-title text-sm">Stato NAV-FI Asset AI</h2>

                    <ul class="list-disc list-inside text-xs">
                        <li>
                            Reachable:
                            <span class="{{ not navfi_reachable ? 'text-warning' : 'text-success' }}">
                                {{ navfi_reachable ? 'Yes' : 'No' }}
                            </span>
                        <li>
                            Test mode:
                            <span class="{{ test_mode ? 'text-warning' : 'text-success' }}">
                                {{ test_mode ? 'Active (no call to Engine NAV-FI)' : 'Disabled' }}
                            </span>
                        </li>
                        <li>
                            Offline fallback:
                            <span class="{{ offline_fallback ? 'text-success' : 'text-warning' }}">
                                {{ offline_fallback ? 'Active (use only DB is the engine NAV-FI do not respond)' : 'Disabled' }}
                            </span>
                        </li>
                    </ul>

                    <div class="divider my-1"></div>

                </div>
            </div>
        </div>
    </div>
{% endblock %}
