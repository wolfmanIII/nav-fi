---
description: Check project health (Linting)
---
Running Symfony linting commands to verify container, YAML, and Twig files.

// turbo
1. Lint Service Container: `php bin/console lint:container`
// turbo
2. Lint YAML config: `php bin/console lint:yaml config`
// turbo
3. Lint Twig templates: `php bin/console lint:twig templates`
